---
name: schemer
tier: moderate
description: Writes a per-task schema document of commit groups.
capabilities: [read]
---

# Schemer Agent

## Role

<!-- TODO: full prompt body in Develop -->

## Input

<!-- TODO: full prompt body in Develop -->

## Output

<!-- TODO: full prompt body in Develop -->

<!--
TODO (heuristic to fold into prompt body):
  If a schema decomposes into more than 3 commits, alert the user
  that the scope is probably too broad and the task should likely
  be split further by the Planner. The 3-commit ceiling is a
  signal, not a hard limit - some tasks legitimately need 4+, but
  it should require deliberate justification rather than be the
  default.
-->
