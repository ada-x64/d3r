---
repo: <owner/repo>
created: <ISO Date>
tags: [] # things like project area, 'blocked:ISSUE_REF', estimates, etc.
---

## Problem

A brief description of the problem, no more than 2000 words. Average 1000 unless
more context is needed. This may include tables for shared context or expository
code changes.

<!--
This template describes an issue with _child entries._
These are reserved for larger tasks, usually new features
or major rewrites.

Umbrella issues live in `issues/.umbrellas/<umbrella-name>.md`.
They are excluded from kanban WIP counts.

Children live grouped under `<column>/<umbrella-name>/<child>.md`
and move through kanban columns independently. The umbrella name
is implicit from the parent directory; no explicit `children:` or
`status:` field is needed.

When all current children are closed, the umbrella stays open
until the operator decides it's done (more children may surface);
done umbrellas move to the archive.
-->
