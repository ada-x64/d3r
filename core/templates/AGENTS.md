The vault is single-branch (`main`), append-only, and commit-as-you-go. Do not
create branches inside the vault. Push after changes.

This `AGENTS.md` cascades: agents working anywhere under this directory honor
its rules; subdirectories MAY layer their own `AGENTS.md` to add or narrow
guidance, but MUST NOT relax the rules above.

You MUST read sibling `README.md` and `d3r.md` before making changes.
