When writing tasks, format the task's branch name to match the convention in
../../AGENTS.md.

The vault itself is single-branch (`main`), append-only, and commit-as-you-go.
Do not create branches inside the vault. Push after changes.

You MUST read the README.md and d3r.md before making changes.
