{
  "name": "d3r",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "lint": "oxlint --deny-warnings",
    "lint:md": "markdownlint-cli2",
    "fmt": "prettier --write .",
    "fmt:check": "prettier --check .",
    "test": "vitest --run --passWithNoTests",
    "test:watch": "vitest",
    "test:dist": "vitest --run --project=dist --passWithNoTests",
    "test:coverage": "vitest --run --coverage --passWithNoTests",
    "cli": "node cli/dist/cli.js",
    "clean": "rimraf --glob 'dist' './{,*/,adapters/*/}{dist,tsconfig*.tsbuildinfo}'"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@vitest/coverage-v8": "^3.2.4",
    "gray-matter": "^4",
    "markdownlint-cli2": "^0.22.1",
    "memfs": "^4",
    "msw": "^2",
    "oxlint": "^1.61.0",
    "prettier": "^3.8.3",
    "rimraf": "^6.1.3",
    "tsx": "^4",
    "typescript": "^5",
    "vitest": "^3.2.4",
    "yaml": "^2",
    "zod": "^3"
  }
}
